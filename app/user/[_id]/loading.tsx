import { Card } from '@/components/Card';

// goal with this file is to represent the lookalike of the actual component
const Loading = () => (
  <Card
    title="Loading..."
    imageAlt="Loading..."
    content="Content is loading..."
  />
);

export default Loading;
