import { CardContainer } from '@/components/Card';

export const UserCardFailed = () => (
  <CardContainer>
    <h2>User not found</h2>
    <p>Sorry, we couldn&apos;t find the user you are looking for.</p>
  </CardContainer>
);
